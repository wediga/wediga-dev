[project]
name = "wediga-backend"
version = "0.1.0"
description = "FastAPI backend for the wediga.dev portfolio site"
requires-python = ">=3.12"
dependencies = [
    "argon2-cffi>=25.1.0",
    "cryptography>=49.0.0",
    "fastapi[standard]>=0.136.3",
    "httpx>=0.28.1",
    "itsdangerous>=2.2.0",
    "pyotp>=2.10.0",
    "qrcode>=8.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
