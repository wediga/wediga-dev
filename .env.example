# Backend
SESSION_SECRET=
ADMIN_PASSWORD=
# Set to false for local http development so the session cookie round-trips.
COOKIE_SECURE=

# Frontend
# Server-side URL of the backend. In compose this is http://backend:8000,
# for local development it defaults to http://localhost:8000.
BACKEND_URL=
